{
  "name": "better-organized-brain",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "validate:rust": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml --locked",
    "validate": "npm run build && npm run validate:rust",
    "package:windows": "cargo metadata --manifest-path src-tauri/Cargo.toml --locked --format-version 1 && cargo clean --manifest-path src-tauri/Cargo.toml -p bob && tauri build --bundles nsis",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
